version: "3.8"
services:
  rag:
    build: .
    ports:
      - "8000:8000"
    environment:
      - PORT=8000
      - FAISS_INDEX_PATH=/data/faiss.index
      - EMBED_DIM=1536
    volumes:
      - ./data:/data
